import React, { useRef, useEffect, useState } from 'react';

import mapboxgl from '!mapbox-gl'; // eslint-disable-line import/no-webpack-loader-syntax

export default function Filters({ map }) {
	return (
		<div className="filter-container">
			Filter
		</div>
	);
}